# Plate 24

Make **24** from the 4 digits on a New Jersey or New York license plate.

Supports: `+` `−` `×` `÷` `mod` `^` `√` `!`

---

## Deploy free on Cloudflare Pages

**Option A — GitHub (recommended, auto-redeploys on push):**

1. Push this repo to GitHub
2. Go to [pages.cloudflare.com](https://pages.cloudflare.com) → **Create a project** → **Connect to Git**
3. Select the repo
4. Build command: *(leave blank)*
5. Build output directory: `/` (or leave blank)
6. Click **Save and Deploy**

You get a free `*.pages.dev` URL instantly. Connect a custom domain for free.

**Option B — CLI one-shot deploy:**

```bash
npm install -g wrangler
wrangler pages deploy . --project-name plate-24
```

---

dedicated to 76 117 99 121.
